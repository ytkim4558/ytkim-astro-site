<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="3.10.0">Jekyll</generator><link href="https://ytkim4558.github.io/feed.xml" rel="self" type="application/atom+xml" /><link href="https://ytkim4558.github.io/" rel="alternate" type="text/html" /><updated>2026-05-30T02:32:24+00:00</updated><id>https://ytkim4558.github.io/feed.xml</id><title type="html">Yongtak Kim Engineering Notes</title><subtitle>Cloud Analytics, Data Platform, and AI Workflow</subtitle><entry><title type="html">Windows Terminal이 열리자마자 죽을 때 — DCOM 10010 에러와 재설치로 해결</title><link href="https://ytkim4558.github.io/windows-terminal-crash-fix" rel="alternate" type="text/html" title="Windows Terminal이 열리자마자 죽을 때 — DCOM 10010 에러와 재설치로 해결" /><published>2026-05-30T09:00:00+00:00</published><updated>2026-05-30T09:00:00+00:00</updated><id>https://ytkim4558.github.io/windows-terminal-crash-fix</id><content type="html" xml:base="https://ytkim4558.github.io/windows-terminal-crash-fix"><![CDATA[<p>Windows 11에서 Windows Terminal이 실행 직후 조용히 종료되는 문제를 겪었다. 마우스 포인터에 로딩 원이 잠깐 돌다가 사라지고, 창은 열리지 않는다. PowerShell도 기본 터미널이 WT로 설정돼 있어서 같이 안 열렸다.</p>

<h2 id="증상">증상</h2>

<ul>
  <li>시작 메뉴에서 PowerShell / Terminal 클릭 → 로딩 커서만 잠깐 뜨고 사라짐</li>
  <li><code class="language-plaintext highlighter-rouge">Win+R</code> → <code class="language-plaintext highlighter-rouge">wt</code> → 동일 증상</li>
  <li>이벤트 뷰어 System 로그에 <strong>Event ID 10010</strong> 반복:
    <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>The server {E12CFF52-A866-4C77-9A90-F570A7AA2C6B} did not register with DCOM within the required timeout.
</code></pre></div>    </div>
  </li>
  <li><code class="language-plaintext highlighter-rouge">Start-Process wt -PassThru</code> 실행 시 프로세스가 exit code 0으로 즉시 종료 (크래시가 아닌 정상 종료 코드)</li>
</ul>

<h2 id="원인">원인</h2>

<p><code class="language-plaintext highlighter-rouge">{E12CFF52-A866-4C77-9A90-F570A7AA2C6B}</code>는 Windows Terminal의 DelegationTerminal CLSID다. WT가 DCOM에 제대로 등록되지 못해 시스템이 타임아웃을 발생시킨 것.</p>

<p>직접적 원인은 <code class="language-plaintext highlighter-rouge">settings.json</code>에 유효하지 않은 값이 들어간 것이었다:</p>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nl">"rendering.graphicsAPI"</span><span class="p">:</span><span class="w"> </span><span class="s2">"software"</span><span class="w">
</span></code></pre></div></div>

<p>WT가 허용하는 값은 <code class="language-plaintext highlighter-rouge">automatic | direct2d | direct3d11</code>뿐이다. 잘못된 값이 들어가면 WT는 설정 파싱 단계에서 실패하고 조용히 종료한다.</p>

<h2 id="시도한-것들-효과-없음">시도한 것들 (효과 없음)</h2>

<table>
  <thead>
    <tr>
      <th>시도</th>
      <th>결과</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">chcp 65001</code></td>
      <td>폰트 깨짐과 무관</td>
    </tr>
    <tr>
      <td>settings.json에서 잘못된 키 제거</td>
      <td>여전히 안 열림</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Reset-AppxPackage</code></td>
      <td>여전히 안 열림</td>
    </tr>
    <tr>
      <td>state.json 삭제</td>
      <td>여전히 안 열림</td>
    </tr>
  </tbody>
</table>

<h2 id="해결-완전-재설치">해결: 완전 재설치</h2>

<div class="language-powershell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 1. 제거</span><span class="w">
</span><span class="n">Get-AppxPackage</span><span class="w"> </span><span class="nx">Microsoft.WindowsTerminal</span><span class="w"> </span><span class="o">|</span><span class="w"> </span><span class="n">Remove-AppxPackage</span><span class="w">

</span><span class="c"># 2. 재등록</span><span class="w">
</span><span class="n">Add-AppxPackage</span><span class="w"> </span><span class="nt">-RegisterByFamilyName</span><span class="w"> </span><span class="nt">-MainPackage</span><span class="w"> </span><span class="nx">Microsoft.WindowsTerminal_8wekyb3d8bbwe</span><span class="w">
</span></code></pre></div></div>

<p>재설치 후 정상 실행 확인.</p>

<h2 id="임시-우회-기본-터미널을-conhost로-변경">임시 우회: 기본 터미널을 conhost로 변경</h2>

<p>WT가 안 열리는 동안 PowerShell을 쓰려면 기본 터미널을 Windows Console Host로 변경한다:</p>

<div class="language-powershell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$reg</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"HKCU:\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer\Advanced"</span><span class="w">
</span><span class="n">Set-ItemProperty</span><span class="w"> </span><span class="nv">$reg</span><span class="w"> </span><span class="nt">-Name</span><span class="w"> </span><span class="s1">'DelegationConsole'</span><span class="w"> </span><span class="nt">-Value</span><span class="w"> </span><span class="s1">'{2EACA947-7F5F-4CFA-BA87-8F7FBEEFBE69}'</span><span class="w">
</span><span class="n">Set-ItemProperty</span><span class="w"> </span><span class="nv">$reg</span><span class="w"> </span><span class="nt">-Name</span><span class="w"> </span><span class="s1">'DelegationTerminal'</span><span class="w"> </span><span class="nt">-Value</span><span class="w"> </span><span class="s1">'{2EACA947-7F5F-4CFA-BA87-8F7FBEEFBE69}'</span><span class="w">
</span></code></pre></div></div>

<p>복원할 때:</p>

<div class="language-powershell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">Set-ItemProperty</span><span class="w"> </span><span class="nv">$reg</span><span class="w"> </span><span class="nt">-Name</span><span class="w"> </span><span class="s1">'DelegationConsole'</span><span class="w"> </span><span class="nt">-Value</span><span class="w"> </span><span class="s1">'{2EACA947-7F5F-4CFA-BA87-8F7FBEEFBE69}'</span><span class="w">
</span><span class="n">Set-ItemProperty</span><span class="w"> </span><span class="nv">$reg</span><span class="w"> </span><span class="nt">-Name</span><span class="w"> </span><span class="s1">'DelegationTerminal'</span><span class="w"> </span><span class="nt">-Value</span><span class="w"> </span><span class="s1">'{E12CFF52-A866-4C77-9A90-F570A7AA2C6B}'</span><span class="w">
</span></code></pre></div></div>

<h2 id="교훈">교훈</h2>

<ul>
  <li>WT의 <code class="language-plaintext highlighter-rouge">settings.json</code>에 유효하지 않은 값을 넣으면 <strong>에러 팝업 한 번 보여주고 기본 설정으로 폴백</strong>하는 게 아니라, 상황에 따라 <strong>아예 실행이 안 될 수 있다</strong>.</li>
  <li><code class="language-plaintext highlighter-rouge">Reset-AppxPackage</code>로 부족하면 <code class="language-plaintext highlighter-rouge">Remove</code> → <code class="language-plaintext highlighter-rouge">Add</code>로 완전 재설치해야 한다.</li>
  <li>DCOM 10010 에러에서 CLSID가 <code class="language-plaintext highlighter-rouge">{E12CFF52-...}</code>이면 Windows Terminal 문제를 의심할 것.</li>
</ul>

<h2 id="환경">환경</h2>

<ul>
  <li>Windows 11 Pro 25H2</li>
  <li>Windows Terminal 1.24.11321.0</li>
  <li>NVIDIA RTX 5090, 드라이버 596.49</li>
</ul>]]></content><author><name>김용탁</name></author><category term="troubleshooting" /><summary type="html"><![CDATA[Windows 11에서 Windows Terminal이 실행 직후 조용히 종료되는 문제를 겪었다. 마우스 포인터에 로딩 원이 잠깐 돌다가 사라지고, 창은 열리지 않는다. PowerShell도 기본 터미널이 WT로 설정돼 있어서 같이 안 열렸다.]]></summary></entry><entry><title type="html">GitHub Pages 포트폴리오 모바일 레이아웃을 적응형으로 정리한 이유</title><link href="https://ytkim4558.github.io/adaptive-mobile-layout-handoff" rel="alternate" type="text/html" title="GitHub Pages 포트폴리오 모바일 레이아웃을 적응형으로 정리한 이유" /><published>2026-05-30T00:00:00+00:00</published><updated>2026-05-30T00:00:00+00:00</updated><id>https://ytkim4558.github.io/adaptive-mobile-layout-handoff</id><content type="html" xml:base="https://ytkim4558.github.io/adaptive-mobile-layout-handoff"><![CDATA[<p><code class="language-plaintext highlighter-rouge">ytkim4558.github.io</code>의 포트폴리오와 AI Workflow 글 상세 페이지를 보다가 상단 레이아웃이 서로 맞지 않는 문제가 드러났다.</p>

<p>문제는 단순히 이미지가 마음에 들지 않는 수준이 아니었다. <code class="language-plaintext highlighter-rouge">/en/portfolio/</code>는 큰 포트폴리오 히어로와 별도의 cover 이미지가 겹쳐 보였고, <code class="language-plaintext highlighter-rouge">/linkedin-api-permission-check</code> 같은 글 상세 페이지는 상단에 비어 보이는 영역이 생겼다. <code class="language-plaintext highlighter-rouge">/tag/ai-workflow/</code>는 또 다른 방식으로 큰 헤더와 카드 목록이 이어져 전체 사이트가 한 벌의 UI처럼 보이지 않았다.</p>

<h2 id="처음-놓친-것">처음 놓친 것</h2>

<p>처음에는 CSS 몇 줄로 상단 여백과 cover 이미지를 줄이면 된다고 봤다.
하지만 그 접근은 부족했다.</p>

<ol>
  <li>공개 URL은 로컬 수정만으로 바뀌지 않는다.</li>
  <li>이 블로그는 <code class="language-plaintext highlighter-rouge">GitHubPageMaker</code> 소스 repo와 <code class="language-plaintext highlighter-rouge">ytkim4558.github.io</code> 배포 repo가 분리되어 있다.</li>
  <li>정식 반영은 Docker 기반 Jekyll build 후 <code class="language-plaintext highlighter-rouge">output/</code>을 배포 repo로 복사해야 한다.</li>
  <li>기존 repo에는 이미 “다른 AI 에이전트에게 남기는 메모”와 cross-agent handoff 규칙이 있었다.</li>
</ol>

<p>특히 4번을 바로 찾지 못한 것이 이번 작업의 핵심 실수였다.
<code class="language-plaintext highlighter-rouge">AGENTS.md</code>만 보고 멈추지 말고, <code class="language-plaintext highlighter-rouge">_posts/</code>와 git history에서 기존 인수인계 패턴을 먼저 검색했어야 했다.</p>

<h2 id="반응형과-적응형-중-무엇을-골랐나">반응형과 적응형 중 무엇을 골랐나</h2>

<p>처음에는 일반적인 반응형 수정을 생각했다.
하지만 이 사이트는 이미 Jasper2 기반 데스크톱 레이아웃 위에 포트폴리오 히어로, 태그 페이지, 글 상세 스타일이 여러 겹 쌓인 상태였다.</p>

<p>단순 반응형은 하나의 레이아웃을 유동적으로 줄이는 방식에 가깝다.
반면 적응형은 특정 화면 폭에서 별도 레이아웃을 주는 방식에 가깝다.</p>

<p>이 사이트에는 둘을 섞는 방식이 맞다.</p>

<ul>
  <li>본문, 이미지, 카드 목록은 반응형으로 둔다.</li>
  <li>글 상세 상단, 태그 상단, 포트폴리오 히어로는 모바일에서 적응형 레이아웃처럼 따로 잡는다.</li>
</ul>

<p>검색으로도 이 판단을 보강했다.</p>

<ul>
  <li><a href="https://web.dev/responsive-web-design-basics/">web.dev Responsive web design basics</a>는 모바일 페이지에 viewport meta가 필요하고, viewport보다 넓은 콘텐츠가 가로 스크롤을 만들면 나쁜 사용자 경험이라고 설명한다.</li>
  <li><a href="https://web.dev/learn/design/intro">web.dev Learn Design introduction</a>은 adaptive layout을 media query와 fixed-width layout의 조합으로, responsive design을 media query와 liquid layout의 조합으로 설명한다.</li>
  <li><a href="https://developer.mozilla.org/en-US/docs/Web/CSS/Guides/Media_queries">MDN CSS media queries</a>는 viewport size 기준 media query가 서로 다른 화면 크기에 다른 레이아웃을 주는 핵심 도구라고 설명한다.</li>
</ul>

<p>그래서 최종 선택은 “모바일 적응형 레이어를 가진 반응형 사이트”다.</p>

<h2 id="실제-수정-방향">실제 수정 방향</h2>

<p><code class="language-plaintext highlighter-rouge">_layouts/default.html</code>에는 이미 다음 viewport meta가 있었다.</p>

<div class="language-html highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;meta</span> <span class="na">name=</span><span class="s">"viewport"</span> <span class="na">content=</span><span class="s">"width=device-width, initial-scale=1.0"</span> <span class="nt">/&gt;</span>
</code></pre></div></div>

<p>따라서 기기의 가로폭을 읽는 기본 설정은 있었다.
문제는 CSS였다. 데스크톱 기준의 큰 width, hero, title 규칙이 모바일에서도 남아 있었다.</p>

<p>수정은 다음 방향으로 진행했다.</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">@media (max-width: 600px)</code>를 모바일 적응형 구간으로 본다.</li>
  <li><code class="language-plaintext highlighter-rouge">.post-full-header</code>, <code class="language-plaintext highlighter-rouge">.post-full-image</code>, <code class="language-plaintext highlighter-rouge">.post-full-content</code>, <code class="language-plaintext highlighter-rouge">.portfolio-hero</code>, <code class="language-plaintext highlighter-rouge">.portfolio-metrics</code>, <code class="language-plaintext highlighter-rouge">.portfolio-toc</code>는 데스크톱 부모 폭을 상속하지 않게 한다.</li>
  <li>모바일에서는 주요 블록을 <code class="language-plaintext highlighter-rouge">calc(100vw - ...)</code> 기준으로 viewport 안에 고정한다.</li>
  <li>긴 한국어/영문 혼합 제목은 모바일에서 더 작은 고정 크기와 줄바꿈 규칙을 사용한다.</li>
  <li>포트폴리오 페이지는 자체 hero가 있으므로 generic page cover figure와 <code class="language-plaintext highlighter-rouge">Portfolio</code> 중복 제목을 숨긴다.</li>
</ul>

<h2 id="빌드와-확인">빌드와 확인</h2>

<p>Docker Desktop은 설치되어 있었지만 처음에는 <code class="language-plaintext highlighter-rouge">docker-desktop</code> WSL 배포판이 stopped 상태였다.
그래서 Docker Desktop을 실행한 뒤 <code class="language-plaintext highlighter-rouge">docker info</code>가 성공하는 것을 확인하고, Ruby 3.2 컨테이너로 Jekyll build를 돌렸다.</p>

<div class="language-powershell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">docker</span><span class="w"> </span><span class="nx">run</span><span class="w"> </span><span class="nt">--rm</span><span class="w"> </span><span class="se">`
</span><span class="w">  </span><span class="nt">-v</span><span class="w"> </span><span class="nx">jekyll_bundle_232_ruby32_bundler269:/usr/local/bundle</span><span class="w"> </span><span class="se">`
</span><span class="w">  </span><span class="nt">-v</span><span class="w"> </span><span class="s2">"C:\Users\ytkim\projects\GitHubPageMaker:/srv/jekyll"</span><span class="w"> </span><span class="se">`
</span><span class="w">  </span><span class="nt">-w</span><span class="w"> </span><span class="nx">/srv/jekyll</span><span class="w"> </span><span class="se">`
</span><span class="w">  </span><span class="nx">ruby:3.2</span><span class="w"> </span><span class="se">`
</span><span class="w">  </span><span class="nx">bash</span><span class="w"> </span><span class="nt">-lc</span><span class="w"> </span><span class="s2">"bundle _2.6.9_ exec jekyll build --future"</span><span class="w">
</span></code></pre></div></div>

<p>빌드 후 <code class="language-plaintext highlighter-rouge">output/</code>을 <code class="language-plaintext highlighter-rouge">C:\Users\ytkim\projects\ytkim4558.github.io</code>로 복사했다.</p>

<p>검증은 Chrome headless 캡처로 진행했다.</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">http://127.0.0.1:8008/linkedin-api-permission-check.html</code></li>
  <li><code class="language-plaintext highlighter-rouge">http://127.0.0.1:8008/en/portfolio/</code></li>
  <li><code class="language-plaintext highlighter-rouge">http://127.0.0.1:8008/tag/ai-workflow/</code></li>
</ul>

<h2 id="규칙으로-남긴-것">규칙으로 남긴 것</h2>

<p>이번 작업에서 중요한 것은 CSS 자체보다 다음 규칙이다.</p>

<ol>
  <li>공개 GitHub Pages URL을 보고 “안 바뀐다”고 판단하지 않는다. 로컬 수정, build output, 배포 repo, 원격 GitHub Pages는 서로 다른 상태다.</li>
  <li>UI/배포/빌드 의사결정이 생기면 <code class="language-plaintext highlighter-rouge">AGENTS.md</code>만 보지 말고 <code class="language-plaintext highlighter-rouge">_posts/</code>와 git history의 기존 인수인계 패턴을 검색한다.</li>
  <li>사용자가 “왜 이 선택을 했는지 기록하라”고 말하기 전에, 비슷한 작업이면 AI Workflow 글 또는 handoff block을 먼저 제안한다.</li>
  <li>이 규칙은 공개 repo 전반에 적용한다.</li>
  <li>사용자가 중간에 새 지시를 해도 진행 중이던 build, copy, verification, cleanup 단계를 버리지 않는다. 새 지시는 작업 큐에 끼워 넣고, 끊긴 단계는 다시 이어간다.</li>
</ol>

<h2 id="다른-ai-에이전트에게-남기는-메모">다른 AI 에이전트에게 남기는 메모</h2>

<p>이 작업을 이어받는 AI 에이전트는 이 사이트를 단순한 반응형 CSS 문제로 보지 말고, <strong>소스 repo와 배포 repo가 분리된 Jekyll 정적 사이트의 모바일 적응형 레이아웃 작업</strong>으로 봐야 한다.</p>

<ul>
  <li>소스 repo: <code class="language-plaintext highlighter-rouge">C:\Users\ytkim\projects\GitHubPageMaker</code></li>
  <li>배포 repo: <code class="language-plaintext highlighter-rouge">C:\Users\ytkim\projects\ytkim4558.github.io</code></li>
  <li>모바일 레이아웃 최종 오버라이드는 <code class="language-plaintext highlighter-rouge">assets/css/screen.edited.css</code>와 <code class="language-plaintext highlighter-rouge">assets/built/screen.edited.css</code> 둘 다에 있어야 한다.</li>
  <li>build는 Docker Ruby 3.2와 Bundler 2.6.9 기준으로 한다.</li>
  <li>Docker가 실패하면 먼저 Docker Desktop이 켜져 있는지와 <code class="language-plaintext highlighter-rouge">docker info</code>를 확인한다.</li>
  <li>공개 URL은 commit/push 후 GitHub Pages 반영까지 시간이 걸릴 수 있다.</li>
  <li>다음에 비슷한 의사결정이 생기면 먼저 <code class="language-plaintext highlighter-rouge">rg -n "인수인계|handoff|다른 AI 에이전트|Cross-Agent|AI Workflow" .</code>와 <code class="language-plaintext highlighter-rouge">git log --all --grep=handoff --grep=인수인계 --grep=agent --oneline --decorate --max-count=30</code>를 실행한다.</li>
</ul>]]></content><author><name>김용탁</name></author><category term="ai-workflow" /><summary type="html"><![CDATA[ytkim4558.github.io의 포트폴리오와 AI Workflow 글 상세 페이지를 보다가 상단 레이아웃이 서로 맞지 않는 문제가 드러났다.]]></summary></entry><entry><title type="html">LinkedIn API로 기존 글을 읽어 톤을 맞추려다 확인한 것</title><link href="https://ytkim4558.github.io/linkedin-api-permission-check" rel="alternate" type="text/html" title="LinkedIn API로 기존 글을 읽어 톤을 맞추려다 확인한 것" /><published>2026-05-18T00:45:00+00:00</published><updated>2026-05-18T00:45:00+00:00</updated><id>https://ytkim4558.github.io/linkedin-api-permission-check</id><content type="html" xml:base="https://ytkim4558.github.io/linkedin-api-permission-check"><![CDATA[<p>LinkedIn에 새 글을 올리기 전에 기존에 내가 썼던 글의 톤을 보고 맞추고 싶었다.
처음에는 GitHub API처럼 OAuth만 연결하면 내 게시글도 바로 읽을 수 있을 것이라고 생각했다.</p>

<p>하지만 직접 LinkedIn Developer 앱을 만들고 OAuth를 붙여보니, 결론은 조금 달랐다.</p>

<h2 id="목표">목표</h2>

<p>목표는 단순했다.</p>

<ul>
  <li>LinkedIn Developer 앱 생성</li>
  <li>OAuth로 본인 계정 인증</li>
  <li>기존 게시글 조회</li>
  <li>기존 글의 문장 길이, 말투, 한영 병기 방식, 해시태그 사용 방식을 분석</li>
  <li>새 LinkedIn 포스트 초안을 그 톤에 맞게 작성</li>
</ul>

<p>이미 <a href="https://github.com/ytkim4558/linkedin-posting-mcp"><code class="language-plaintext highlighter-rouge">linkedin-posting-mcp</code></a>라는 로컬 MCP 서버를 만들어둔 상태였기 때문에,
여기에 게시글 조회 도구를 추가해 실제로 가능한지 확인했다.</p>

<h2 id="구현한-것">구현한 것</h2>

<p><code class="language-plaintext highlighter-rouge">linkedin-posting-mcp</code>에 다음 기능을 추가했다.</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">linkedin_get_my_posts</code> MCP tool</li>
  <li><code class="language-plaintext highlighter-rouge">auth:url:signin</code>: <code class="language-plaintext highlighter-rouge">openid profile email</code>만 요청하는 최소 로그인 테스트</li>
  <li><code class="language-plaintext highlighter-rouge">auth:url</code>: <code class="language-plaintext highlighter-rouge">openid profile email w_member_social</code> 요청</li>
  <li><code class="language-plaintext highlighter-rouge">auth:url:read</code>: <code class="language-plaintext highlighter-rouge">r_member_social</code>까지 포함해 기존 게시글 조회 가능성 테스트</li>
  <li><code class="language-plaintext highlighter-rouge">posts:me</code>: 인증된 사용자의 최근 게시글 조회 테스트</li>
</ul>

<p>조회 요청은 LinkedIn Posts API의 author finder 형태로 구성했다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>GET /rest/posts?author=urn:li:person:{member_id}&amp;q=author
</code></pre></div></div>

<h2 id="실제로-확인한-흐름">실제로 확인한 흐름</h2>

<p>먼저 LinkedIn Developer 앱을 만들었다.
앱 생성에는 연결할 LinkedIn Page가 필요했고, 개인 기술 노트용으로 <code class="language-plaintext highlighter-rouge">Yongtak Engineering Notes</code> 페이지를 만들었다.</p>

<p>이 과정에서 생각보다 많은 작은 판단이 필요했다.</p>

<h2 id="중간에-헷갈렸던-지점들">중간에 헷갈렸던 지점들</h2>

<p>첫 번째는 <strong>LinkedIn Page 종류</strong>였다.</p>

<p>처음에는 앱 생성 화면에서 <code class="language-plaintext highlighter-rouge">Create a new LinkedIn Page</code>를 눌렀고, <code class="language-plaintext highlighter-rouge">linkedin.com/showcase/</code> 형태의 페이지 생성 화면으로 들어갔다.
하지만 Showcase page는 기존 회사 페이지의 하위 페이지라서 <code class="language-plaintext highlighter-rouge">연결된 단체 페이지</code>가 필수였다.
개인 개발자 앱 연결용으로는 맞지 않았다.</p>

<p>결국 <code class="language-plaintext highlighter-rouge">LinkedIn 페이지 만들기</code> 화면에서 <code class="language-plaintext highlighter-rouge">회사</code> 유형을 골라 일반 Company Page를 만들었다.
판단 기준은 단순했다.</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">linkedin.com/showcase/</code>가 보이면 하위 브랜드/쇼케이스 페이지라서 이번 목적과 맞지 않다.</li>
  <li><code class="language-plaintext highlighter-rouge">linkedin.com/company/</code>가 보이면 Developer App에 연결할 일반 페이지로 쓸 수 있다.</li>
</ul>

<p>두 번째는 <strong>앱 이름</strong>이었다.</p>

<p>처음 생각한 이름은 <code class="language-plaintext highlighter-rouge">Yongtak LinkedIn Workflow</code>였지만, LinkedIn Developer 앱 이름에는 <code class="language-plaintext highlighter-rouge">LinkedIn</code>이라는 단어를 넣을 수 없었다.
그래서 앱 이름은 <code class="language-plaintext highlighter-rouge">Yongtak AI Workflow</code>처럼 바꿔야 했다.</p>

<p>세 번째는 <strong>회사 페이지 약관 체크</strong>였다.</p>

<p>화면에는 “회사 페이지의 공식 담당자로서 회사를 대표해서 회사 페이지를 관리할 권리가 있음을 확인한다”는 문구가 나온다.
실제 회사나 이전 회사 이름으로 만들면 애매하지만, <code class="language-plaintext highlighter-rouge">Yongtak Engineering Notes</code>처럼 본인이 직접 만든 개인 기술 노트 페이지라면 본인이 관리 주체이므로 진행할 수 있다고 판단했다.</p>

<p>네 번째는 <strong>앱 로고 조건</strong>이었다.</p>

<p>LinkedIn Developer 앱은 유효한 app logo를 요구했다.
기존 이미지가 조건에 안 맞을 수 있어서 300x300 PNG를 별도로 만들었다.
이런 로고는 기능에는 중요하지 않지만, OAuth 동의 화면에 노출되므로 최소한 구분 가능한 형태가 필요했다.</p>

<p>다섯 번째는 <strong>PowerShell 환경변수 입력</strong>이었다.</p>

<p>처음에는 <code class="language-plaintext highlighter-rouge">SetEnvironmentVariable</code>에 값을 직접 넣으려다 쉼표 누락과 특수문자 때문에 문법 오류가 났다.
그래서 테스트 단계에서는 사용자 환경변수에 저장하지 않고, 현재 PowerShell 세션에만 넣는 방식으로 바꿨다.</p>

<div class="language-powershell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$</span><span class="nn">env</span><span class="p">:</span><span class="nv">LINKEDIN_CLIENT_ID</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">Read-Host</span><span class="w"> </span><span class="s2">"LinkedIn Client ID"</span><span class="w">
</span><span class="nv">$</span><span class="nn">env</span><span class="p">:</span><span class="nv">LINKEDIN_CLIENT_SECRET</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">Read-Host</span><span class="w"> </span><span class="s2">"LinkedIn Client Secret"</span><span class="w">
</span><span class="nv">$</span><span class="nn">env</span><span class="p">:</span><span class="nv">LINKEDIN_REDIRECT_URI</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s2">"http://localhost:3000/callback"</span><span class="w">
</span></code></pre></div></div>

<p>이 방식은 창을 닫으면 사라지므로 테스트에 적합하고, <code class="language-plaintext highlighter-rouge">Client Secret</code>을 평문 사용자 환경변수에 남기지 않아도 된다.</p>

<p>여섯 번째는 <strong>authorization code 복사 방식</strong>이었다.</p>

<p>callback URL 전체를 넣거나 <code class="language-plaintext highlighter-rouge">code=</code>까지 같이 넣으면 token exchange가 실패한다.
실행해야 하는 값은 오직 <code class="language-plaintext highlighter-rouge">code</code> 값 자체다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>http://localhost:3000/callback?code=AQ...&amp;state=...
</code></pre></div></div>

<p>위 URL이라면 실행은 이렇게 해야 한다.</p>

<div class="language-powershell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">npm</span><span class="w"> </span><span class="nx">run</span><span class="w"> </span><span class="nx">auth:exchange</span><span class="w"> </span><span class="o">--</span><span class="w"> </span><span class="s2">"AQ..."</span><span class="w">
</span></code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">code=</code>나 <code class="language-plaintext highlighter-rouge">&amp;state=...</code>는 넣지 않는다.</p>

<p>일곱 번째는 <strong>Client Secret 노출 대응</strong>이었다.</p>

<p>테스트 중 secret이 채팅에 노출될 수 있다는 점도 확인했다.
이 경우 LinkedIn Developer 앱의 <code class="language-plaintext highlighter-rouge">Generate a new Client Secret</code>으로 새 secret을 만들고, 기존 노출 값을 폐기해야 한다.</p>

<h2 id="오류-메시지별-대응">오류 메시지별 대응</h2>

<p>이번 작업에서 실제로 마주친 오류와 대응은 다음과 같다.</p>

<table>
  <thead>
    <tr>
      <th>오류/증상</th>
      <th>발생한 이유</th>
      <th>대응</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Missing LinkedIn config: LINKEDIN_CLIENT_ID, LINKEDIN_CLIENT_SECRET</code></td>
      <td>현재 PowerShell 세션에 LinkedIn 앱 설정이 없다. <code class="language-plaintext highlighter-rouge">$env:</code>로 넣은 값은 새 창에서 유지되지 않는다.</td>
      <td>같은 PowerShell 창에서 <code class="language-plaintext highlighter-rouge">Read-Host</code>로 다시 입력하거나, User 환경변수로 저장한 뒤 새 창을 연다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Missing ')' in method call</code> / <code class="language-plaintext highlighter-rouge">Unexpected token 'User'</code></td>
      <td>PowerShell <code class="language-plaintext highlighter-rouge">SetEnvironmentVariable()</code> 호출에서 쉼표가 빠졌거나 따옴표가 깨졌다.</td>
      <td>테스트 단계에서는 <code class="language-plaintext highlighter-rouge">SetEnvironmentVariable</code> 대신 <code class="language-plaintext highlighter-rouge">$env:LINKEDIN_CLIENT_ID = Read-Host ...</code> 방식을 쓴다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Bummer, something went wrong. In five seconds, you will be redirected to: localhost</code></td>
      <td>LinkedIn이 <code class="language-plaintext highlighter-rouge">localhost</code> callback으로 redirect하려는데 받을 로컬 서버가 없어서 브라우저가 실패처럼 보인다.</td>
      <td><code class="language-plaintext highlighter-rouge">node -e "http=require('http');http.createServer((q,s)=&gt;{console.log(q.url);s.end('OK')}).listen(3000)"</code>로 임시 callback 서버를 띄운다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">SyntaxError: Invalid or unexpected token</code></td>
      <td><code class="language-plaintext highlighter-rouge">node -e</code> 명령을 PowerShell에 여러 줄로 붙여넣어 JavaScript 문자열이 중간에서 끊겼다.</td>
      <td>짧은 한 줄 명령으로 실행한다. PowerShell 프롬프트가 <code class="language-plaintext highlighter-rouge">&gt;&gt;</code>로 바뀌면 줄이 깨진 것이다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Unable to retrieve access token: authorization code not found</code></td>
      <td><code class="language-plaintext highlighter-rouge">code</code> 값을 잘못 복사했거나, 이미 사용한 authorization code를 다시 썼거나, 시간이 지나 만료됐다.</td>
      <td>OAuth URL을 다시 생성하고 새 code를 즉시 교환한다. <code class="language-plaintext highlighter-rouge">code=</code>와 <code class="language-plaintext highlighter-rouge">&amp;state=</code>는 빼고 값만 넣는다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">ACCESS_DENIED: Not enough permissions to access: partnerApiPostsExternal.FINDER-author</code></td>
      <td>OAuth 로그인과 토큰 발급은 성공했지만, 앱에 Posts Finder API 조회 권한이 없다.</td>
      <td><code class="language-plaintext highlighter-rouge">r_member_social</code>을 포함해 다시 인증해본다. 그래도 같으면 LinkedIn 앱 권한/심사 영역이라 수동 샘플 기반으로 톤 분석한다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">Please upload a valid app logo</code></td>
      <td>앱 로고 파일이 LinkedIn 조건에 맞지 않거나 미리보기 가능한 이미지가 아니다.</td>
      <td>300x300 PNG/JPG를 새로 만들어 업로드한다.</td>
    </tr>
    <tr>
      <td>앱 이름에 <code class="language-plaintext highlighter-rouge">LinkedIn</code>을 넣을 수 없음</td>
      <td>LinkedIn Developer 앱 이름에 <code class="language-plaintext highlighter-rouge">LinkedIn</code> 브랜드 단어 사용이 제한된다.</td>
      <td><code class="language-plaintext highlighter-rouge">Yongtak AI Workflow</code>, <code class="language-plaintext highlighter-rouge">Yongtak Workflow Tools</code>처럼 바꾼다.</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">연결된 단체 페이지</code>가 필수로 나옴</td>
      <td>Showcase page 생성 화면에 들어간 경우다. Showcase는 기존 회사 페이지의 하위 페이지다.</td>
      <td>뒤로 가서 <code class="language-plaintext highlighter-rouge">회사</code> 유형의 일반 Company Page를 만든다. URL이 <code class="language-plaintext highlighter-rouge">linkedin.com/company/...</code>인지 확인한다.</td>
    </tr>
  </tbody>
</table>

<p>이 표를 남기는 이유는 간단하다.
OAuth 문제는 대부분 “권한이 없다”로 뭉뚱그려 보이지만, 실제로는 설정 누락, redirect 서버 부재, code 복사 실수, API 권한 부족이 전부 다른 문제다.
각 단계의 오류를 분리해서 봐야 시간을 덜 버린다.</p>

<p>이후 OAuth redirect URL을 아래처럼 등록했다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>http://localhost:3000/callback
</code></pre></div></div>

<p>초기에는 callback을 받을 로컬 서버가 없어서 브라우저에 <code class="language-plaintext highlighter-rouge">Bummer, something went wrong.</code> 메시지가 보였다.
하지만 이것은 OAuth가 완전히 실패했다기보다, LinkedIn이 <code class="language-plaintext highlighter-rouge">localhost</code>로 redirect하려는데 받을 서버가 없어서 생긴 혼란이었다.</p>

<p>간단한 Node HTTP 서버를 띄우고 다시 시도하니 callback code를 받을 수 있었다.</p>

<div class="language-powershell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">node</span><span class="w"> </span><span class="nt">-e</span><span class="w"> </span><span class="s2">"http=require('http');http.createServer((q,s)=&gt;{console.log(q.url);s.end('OK')}).listen(3000)"</span><span class="w">
</span></code></pre></div></div>

<p>그 다음 authorization code를 access token으로 교환하는 것까지는 성공했다.</p>

<h2 id="막힌-지점">막힌 지점</h2>

<p>문제는 기존 게시글 조회였다.</p>

<p><code class="language-plaintext highlighter-rouge">posts:me</code>를 실행하면 다음 에러가 반환됐다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>LinkedIn API 403:
ACCESS_DENIED
Not enough permissions to access: partnerApiPostsExternal.FINDER-author
</code></pre></div></div>

<p>즉 OAuth 인증 자체는 성공했지만, 이 앱이 Posts Finder API로 본인 게시글을 읽을 권한은 없었다.
<code class="language-plaintext highlighter-rouge">r_member_social</code>을 scope에 포함해 다시 인증해도 결과는 같았다.</p>

<p>이후 공식 FAQ를 다시 확인하니 결론은 더 명확했다.
LinkedIn Marketing API FAQ는 <code class="language-plaintext highlighter-rouge">r_member_social</code>을 closed permission으로 설명하고, 현재 resource constraint 때문에 access request를 받지 않는다고 안내한다.</p>

<p>즉 이 문제는 구현 문법이나 OAuth callback 처리가 아니라, 현재 앱에 발급 가능한 권한의 경계였다.</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">Sign In with LinkedIn using OpenID Connect</code>: 로그인과 기본 프로필 확인</li>
  <li><code class="language-plaintext highlighter-rouge">Share on LinkedIn</code>: <code class="language-plaintext highlighter-rouge">w_member_social</code>, 글 작성 권한</li>
  <li><code class="language-plaintext highlighter-rouge">Community Management API / Posts API</code>: 게시글 조회와 관리 영역</li>
  <li><code class="language-plaintext highlighter-rouge">r_member_social</code>: 개인 게시글 조회에 필요한 권한이지만 closed/restricted</li>
</ul>

<h2 id="결론">결론</h2>

<p>LinkedIn API는 존재하지만, 개인 개발자 앱에서 기존 게시글 조회가 GitHub처럼 바로 열리는 구조는 아니었다.</p>

<p>이번 확인으로 나눈 결론은 다음과 같다.</p>

<ul>
  <li>로그인/OIDC 인증은 가능하다.</li>
  <li>토큰 발급도 가능하다.</li>
  <li>향후 승인된 범위 안에서 게시 워크플로를 만드는 것은 가능하다.</li>
  <li>하지만 기존 게시글 조회는 별도 권한 승인 없이 바로 되지 않았다.</li>
  <li>공식 FAQ 기준으로 <code class="language-plaintext highlighter-rouge">r_member_social</code>은 closed permission이므로, 개인 개발자 앱에서 기존 개인 글을 바로 읽는 경로로 기대하면 안 된다.</li>
  <li>따라서 “기존 LinkedIn 글을 읽어 톤을 분석”하는 작업은 당장은 수동 샘플 기반이 현실적이다.</li>
</ul>

<h2 id="앞으로의-규칙">앞으로의 규칙</h2>

<p>LinkedIn 자동화는 다음 기준으로 다룬다.</p>

<ul>
  <li>브라우저 쿠키, 내부 API, 스크래핑, DM 자동화는 사용하지 않는다.</li>
  <li>공식 OAuth와 문서화된 API만 사용한다.</li>
  <li>기존 글 조회가 필요하면 <code class="language-plaintext highlighter-rouge">r_member_social</code> 또는 Posts Finder 권한이 실제로 승인됐는지 먼저 확인한다.</li>
  <li>권한이 없으면 사용자가 기존 글 2~3개를 직접 제공하고, 그 샘플을 기반으로 톤을 맞춘다.</li>
  <li>Client Secret, authorization code, access token은 채팅과 저장소에 남기지 않는다.</li>
</ul>

<p>이번 삽질의 핵심은 이것이었다.</p>

<p>API가 있다는 것과, 내가 원하는 작업을 지금 내 앱 권한으로 할 수 있다는 것은 다르다.</p>]]></content><author><name>김용탁</name></author><category term="ai-workflow" /><summary type="html"><![CDATA[LinkedIn에 새 글을 올리기 전에 기존에 내가 썼던 글의 톤을 보고 맞추고 싶었다. 처음에는 GitHub API처럼 OAuth만 연결하면 내 게시글도 바로 읽을 수 있을 것이라고 생각했다.]]></summary></entry><entry><title type="html">오래된 Jekyll 블로그의 Dependabot 취약점 33개를 정리한 기록</title><link href="https://ytkim4558.github.io/jekyll-dependabot-cleanup" rel="alternate" type="text/html" title="오래된 Jekyll 블로그의 Dependabot 취약점 33개를 정리한 기록" /><published>2026-05-17T16:10:00+00:00</published><updated>2026-05-17T16:10:00+00:00</updated><id>https://ytkim4558.github.io/jekyll-dependabot-cleanup</id><content type="html" xml:base="https://ytkim4558.github.io/jekyll-dependabot-cleanup"><![CDATA[<p><a href="https://github.com/ytkim4558/codex-resume"><code>codex-resume</code></a> 작업을 포트폴리오와 블로그에 반영하는 과정에서 GitHub push 메시지에 Dependabot 경고가 같이 떴다.
처음에는 단순한 경고처럼 보였지만, 실제로 확인해 보니 오래된 Jekyll 블로그 의존성에서 열린 취약점 알림이 여러 개 쌓여 있었다.</p>

<p>이 글은 그 취약점을 무작정 최신화하지 않고, 사이트가 계속 빌드되는지 검증하면서 정리한 기록이다.</p>

<h2 id="문제를-발견한-위치">문제를 발견한 위치</h2>

<p>블로그는 두 개의 저장소로 나뉘어 있었다.</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">GitHubPageMaker</code>: Jekyll 소스 저장소</li>
  <li><code class="language-plaintext highlighter-rouge">ytkim4558.github.io</code>: 실제 GitHub Pages에 올라가는 생성 결과물 저장소</li>
</ul>

<p>처음에는 <code class="language-plaintext highlighter-rouge">ytkim4558.github.io</code>만 보면 되는 줄 알았지만, 실제 수정해야 할 곳은 Jekyll 소스가 있는 <code class="language-plaintext highlighter-rouge">GitHubPageMaker</code>였다.
생성된 HTML만 직접 고치면 다음 빌드 때 다시 덮어써질 수 있기 때문이다.</p>

<p>그래서 작업 기준을 다음처럼 정했다.</p>

<ul>
  <li>소스 변경은 <code class="language-plaintext highlighter-rouge">GitHubPageMaker</code>에서 한다.</li>
  <li>Jekyll로 <code class="language-plaintext highlighter-rouge">output/</code>을 생성한다.</li>
  <li>생성 결과를 <code class="language-plaintext highlighter-rouge">ytkim4558.github.io</code>에 반영한다.</li>
  <li>공개 URL에서 실제 반영 여부를 확인한다.</li>
</ul>

<h2 id="첫-번째-원인-jekyll-tasks">첫 번째 원인: jekyll-tasks</h2>

<p>Dependabot 알림 중 가장 먼저 정리한 것은 npm 패키지 <code class="language-plaintext highlighter-rouge">jekyll-tasks</code>였다.
이 패키지는 특정 버전만 취약한 것이 아니라 패키지 자체가 malware로 표시되어 있었다.</p>

<p>다행히 현재 블로그 빌드에는 이 패키지가 필요하지 않았다.</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">package.json</code>에는 들어 있었다.</li>
  <li>하지만 실제 Jekyll 빌드는 npm이 아니라 <code class="language-plaintext highlighter-rouge">bundle exec jekyll build</code>로 수행했다.</li>
  <li><code class="language-plaintext highlighter-rouge">gulpfile.js</code>에서도 <code class="language-plaintext highlighter-rouge">require('jekyll-tasks')</code>로 사용하지 않았다.</li>
</ul>

<p>그래서 이 패키지는 제거했다.</p>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nl">"dependencies"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
  </span><span class="nl">"gulp"</span><span class="p">:</span><span class="w"> </span><span class="s2">"github:gulpjs/gulp"</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<p>제거 후 바로 배포하지 않고, Jekyll 빌드가 그대로 성공하는지 먼저 확인했다.</p>

<h2 id="두-번째-원인-오래된-github-pages-런타임">두 번째 원인: 오래된 GitHub Pages 런타임</h2>

<p>Ruby 쪽 알림은 대부분 오래된 <code class="language-plaintext highlighter-rouge">github-pages</code> gem에서 시작됐다.
기존 설정은 다음과 같았다.</p>

<div class="language-ruby highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">gem</span> <span class="s2">"jekyll"</span><span class="p">,</span> <span class="s2">"~&gt; 3.6.2"</span>
<span class="n">gem</span> <span class="s2">"github-pages"</span><span class="p">,</span> <span class="s2">"~&gt; 168"</span>
<span class="n">gem</span> <span class="s2">"rake"</span><span class="p">,</span> <span class="s2">"~&gt; 12.3.0"</span>
</code></pre></div></div>

<p>이 조합은 <code class="language-plaintext highlighter-rouge">nokogiri</code>, <code class="language-plaintext highlighter-rouge">addressable</code>, <code class="language-plaintext highlighter-rouge">activesupport</code>, <code class="language-plaintext highlighter-rouge">tzinfo</code>, <code class="language-plaintext highlighter-rouge">faraday</code> 같은 오래된 하위 의존성을 끌고 왔다.
Dependabot 알림도 대부분 이 하위 의존성에서 발생했다.</p>

<p>처음에는 <code class="language-plaintext highlighter-rouge">github-pages</code>만 최신 계열로 올려도 충분할 것이라고 봤다.</p>

<div class="language-ruby highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">gem</span> <span class="s2">"github-pages"</span><span class="p">,</span> <span class="s2">"~&gt; 232"</span>
</code></pre></div></div>

<p>하지만 이 상태에서는 <code class="language-plaintext highlighter-rouge">nokogiri 1.18.10</code>이 설치되어, 최신 <code class="language-plaintext highlighter-rouge">nokogiri &lt; 1.19.3</code> 알림이 남을 수 있었다.
그래서 GitHub Pages 제약 범위 안에서 <code class="language-plaintext highlighter-rouge">nokogiri</code>를 명시적으로 올렸다.</p>

<div class="language-ruby highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">gem</span> <span class="s2">"github-pages"</span><span class="p">,</span> <span class="s2">"~&gt; 232"</span>
<span class="n">gem</span> <span class="s2">"nokogiri"</span><span class="p">,</span> <span class="s2">"&gt;= 1.19.3"</span>
</code></pre></div></div>

<h2 id="왜-docker로-검증했나">왜 Docker로 검증했나</h2>

<p>Windows 로컬 환경에는 Ruby, Bundler, Jekyll이 설치되어 있지 않았다.
새로 Ruby를 설치하는 대신 Docker로 빌드 환경을 고정했다.</p>

<p>초기에는 <code class="language-plaintext highlighter-rouge">jekyll/jekyll:3.8</code> 이미지를 사용했지만, 최신 <code class="language-plaintext highlighter-rouge">nokogiri 1.19.3</code>은 Ruby 3.2 이상이 필요했다.
그래서 최종 검증은 Ruby 3.2 컨테이너에서 진행했다.</p>

<div class="language-powershell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">docker</span><span class="w"> </span><span class="nx">run</span><span class="w"> </span><span class="nt">--rm</span><span class="w"> </span><span class="se">`
</span><span class="w">  </span><span class="nt">-v</span><span class="w"> </span><span class="nx">jekyll_bundle_232_ruby32_bundler269:/usr/local/bundle</span><span class="w"> </span><span class="se">`
</span><span class="w">  </span><span class="nt">-v</span><span class="w"> </span><span class="s2">"C:\Users\ytkim\projects\GitHubPageMaker:/srv/jekyll"</span><span class="w"> </span><span class="se">`
</span><span class="w">  </span><span class="nt">-w</span><span class="w"> </span><span class="nx">/srv/jekyll</span><span class="w"> </span><span class="se">`
</span><span class="w">  </span><span class="nx">ruby:3.2</span><span class="w"> </span><span class="se">`
</span><span class="w">  </span><span class="nx">bash</span><span class="w"> </span><span class="nt">-lc</span><span class="w"> </span><span class="s2">"gem install bundler -v 2.6.9 &amp;&amp; bundle _2.6.9_ update github-pages nokogiri &amp;&amp; bundle _2.6.9_ exec jekyll build --future"</span><span class="w">
</span></code></pre></div></div>

<p>여기서 <code class="language-plaintext highlighter-rouge">--future</code>를 붙인 이유는 같은 날 작성한 글이 컨테이너 시간대 기준으로 future post로 판단되어 빌드에서 빠질 수 있었기 때문이다.</p>

<h2 id="확인한-것">확인한 것</h2>

<p>업데이트 후에는 다음을 확인했다.</p>

<ul>
  <li>Jekyll 빌드 성공</li>
  <li>포트폴리오 페이지 정상 생성</li>
  <li><code class="language-plaintext highlighter-rouge">codex-resume</code> 글 정상 생성</li>
  <li><code class="language-plaintext highlighter-rouge">AI Workflow</code> 태그 페이지 정상 생성</li>
  <li>기존 상단 메뉴가 새 메뉴로 유지됨</li>
  <li>GitHub Dependabot API 기준 open alert 0개</li>
  <li>공개 URL 응답 200 OK</li>
</ul>

<p>최종적으로 확인한 핵심 URL은 다음과 같다.</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">https://ytkim4558.github.io/portfolio/</code></li>
  <li><code class="language-plaintext highlighter-rouge">https://ytkim4558.github.io/codex-resume-tui-decision</code></li>
  <li><code class="language-plaintext highlighter-rouge">https://ytkim4558.github.io/tag/ai-workflow/</code></li>
</ul>

<h2 id="정리">정리</h2>

<p>이번 작업의 핵심은 단순히 <code class="language-plaintext highlighter-rouge">bundle update</code>를 실행한 것이 아니었다.
오래된 블로그는 의존성만 올리면 깨질 수 있기 때문에, 다음 순서가 중요했다.</p>

<ol>
  <li>실제로 쓰지 않는 malware 패키지부터 제거</li>
  <li>Jekyll 소스 저장소와 생성 결과물 저장소를 분리해서 확인</li>
  <li>Docker로 빌드 환경 고정</li>
  <li>GitHub Pages 런타임 업데이트</li>
  <li>취약 하위 의존성인 <code class="language-plaintext highlighter-rouge">nokogiri</code>를 패치 버전으로 고정</li>
  <li>생성 결과를 공개 페이지에서 검증</li>
</ol>

<p>결과적으로 오래된 Jekyll 블로그를 유지하면서도, GitHub Dependabot 경고를 0개로 줄일 수 있었다.</p>

<p>이런 작업은 눈에 띄는 기능 개발은 아니지만, 운영 중인 개인 사이트를 계속 신뢰 가능한 상태로 유지하는 데 필요한 기본 작업이다.</p>

<h2 id="다른-ai-에이전트에게-남기는-메모">다른 AI 에이전트에게 남기는 메모</h2>

<p>이 블로그를 다음에 수정하는 AI 에이전트는 <code class="language-plaintext highlighter-rouge">ytkim4558.github.io</code>의 HTML을 바로 고치는 대신, 먼저 <code class="language-plaintext highlighter-rouge">GitHubPageMaker</code> 소스를 수정해야 한다.</p>

<ul>
  <li>소스 repo: <code class="language-plaintext highlighter-rouge">C:\Users\ytkim\projects\GitHubPageMaker</code></li>
  <li>배포 repo: <code class="language-plaintext highlighter-rouge">C:\Users\ytkim\projects\ytkim4558.github.io</code></li>
  <li>빌드는 Ruby 3.2 Docker 컨테이너와 Bundler 2.6.9 기준으로 한다.</li>
  <li>같은 날 작성한 글이 빠지지 않도록 <code class="language-plaintext highlighter-rouge">jekyll build --future</code>를 사용한다.</li>
  <li><code class="language-plaintext highlighter-rouge">jekyll-tasks</code>는 malware 알림 원인이므로 다시 추가하지 않는다.</li>
  <li><code class="language-plaintext highlighter-rouge">github-pages ~&gt; 232</code>, <code class="language-plaintext highlighter-rouge">nokogiri &gt;= 1.19.3</code> 제약을 유지한다.</li>
</ul>

<p>자세한 절차는 저장소 루트의 <code class="language-plaintext highlighter-rouge">AGENTS.md</code>에 남겨두는 것이 좋다.</p>]]></content><author><name>김용탁</name></author><category term="ai-workflow" /><summary type="html"><![CDATA[codex-resume 작업을 포트폴리오와 블로그에 반영하는 과정에서 GitHub push 메시지에 Dependabot 경고가 같이 떴다. 처음에는 단순한 경고처럼 보였지만, 실제로 확인해 보니 오래된 Jekyll 블로그 의존성에서 열린 취약점 알림이 여러 개 쌓여 있었다.]]></summary></entry><entry><title type="html">claude-resume TUI를 만들며 마주친 우여곡절 — 인코딩, 키 충돌, 재귀 호출, 스크린샷 모자이크</title><link href="https://ytkim4558.github.io/claude-resume-troubleshooting" rel="alternate" type="text/html" title="claude-resume TUI를 만들며 마주친 우여곡절 — 인코딩, 키 충돌, 재귀 호출, 스크린샷 모자이크" /><published>2026-05-17T16:00:00+00:00</published><updated>2026-05-17T16:00:00+00:00</updated><id>https://ytkim4558.github.io/claude-resume-troubleshooting</id><content type="html" xml:base="https://ytkim4558.github.io/claude-resume-troubleshooting"><![CDATA[<p><a href="https://github.com/ytkim4558/claude-resume"><code class="language-plaintext highlighter-rouge">claude-resume</code></a>는 Claude Code의 기본 <code class="language-plaintext highlighter-rouge">claude --resume</code> 화면을 대체하기 위해 직접 만든 TUI 도구다.
기본 화면은 세션 ID와 시각만 보여주는데, 세션이 쌓이면 “이 ID가 어떤 작업이었지” 가 한눈에 안 들어온다.
첫 메시지 한 줄 미리보기조차 없어서 매번 ID를 일일이 클릭해 확인해야 했다.</p>

<p>해결은 단순했다. <strong>좌측에는 세션 리스트 + 백그라운드 LLM 요약, 우측에는 선택한 세션의 전체 대화</strong> — 두 패널 TUI.</p>

<p><img src="/assets/images/claude-resume/claude-resume-redacted.png" alt="claude-resume의 두 패널 TUI와 LLM 요약 화면" /></p>

<p>말로는 간단하지만 만드는 동안 마주친 결정과 실수가 적지 않았다. 이 글은 그 우여곡절의 기록이다.</p>

<h2 id="핵심-결정-4가지">핵심 결정 4가지</h2>

<h3 id="1-요약은-별도-api-키가-아니라-claude--p-subprocess로">1. 요약은 별도 API 키가 아니라 <code class="language-plaintext highlighter-rouge">claude -p</code> subprocess로</h3>

<p>처음엔 Anthropic API 직접 호출(별도 API 키 + 종량제)을 검토했지만 다음 이유로 기각했다.</p>

<ul>
  <li>별도 결제 발생 (Claude Code Pro 구독비는 API 호출에 안 쓰임)</li>
  <li>API 키 보안 관리 부담 (<code class="language-plaintext highlighter-rouge">keyring</code> 등 추가 셋업 필요)</li>
  <li>호출 빈도가 낮아서 <code class="language-plaintext highlighter-rouge">claude -p</code> 의 6초 지연을 백그라운드 스레드로 흡수 가능</li>
</ul>

<p>결과적으로 <strong>Pro 구독만으로 동작</strong>, 사용자가 추가 설정할 게 없다.</p>

<h3 id="2-정렬용-timestamp는-파일-mtime이-아니라-jsonl-내부-시각">2. 정렬용 timestamp는 파일 mtime이 아니라 JSONL 내부 시각</h3>

<p>세션 정렬·표시용 “마지막 활동” 시각은 JSONL 파일의 mtime이 아니라 <strong>파일 안의 마지막 user/assistant 메시지의 timestamp 필드</strong>를 쓴다.</p>

<p>이유는 사용자 요청이 명확했다 — “LLM 돌렷다고 그 날짜 갱신시키지 말고”. 캐시 파일이나 다른 도구가 파일을 건드려도 정렬 순서가 흔들리지 않아야 한다.</p>

<h3 id="3-재귀-호출-방어--프롬프트-마커">3. 재귀 호출 방어 — 프롬프트 마커</h3>

<p><code class="language-plaintext highlighter-rouge">claude -p "요약해줘 ..."</code> 호출이 그 자체로 <strong>새 Claude Code 세션을 만든다</strong>.
즉 다음번 피커 실행 시 그 요약 호출 세션이 또 피커에 뜨고, 그걸 또 요약하려 하고… 무한 증식.</p>

<p>해결: 요약 프롬프트 첫 줄에 <code class="language-plaintext highlighter-rouge">&lt;&lt;CLAUDE-RESUME-SUMMARY-V1&gt;&gt;</code> 마커를 박고, 세션의 첫 user 메시지에 이 마커가 있으면 리스트에서 제외했다.
레거시(마커 없던 시절) 세션도 프롬프트 접두사 매칭으로 함께 필터했다.</p>

<h3 id="4-cmd--powershell-양쪽에서-동일-명령어로-호출">4. CMD / PowerShell 양쪽에서 동일 명령어로 호출</h3>

<p>처음엔 PowerShell <code class="language-plaintext highlighter-rouge">$PROFILE</code> 함수만 등록했는데 사용자가 곧장 CMD 에서 띄워보고 안 잡힌다고 알려줬다.
결국 세 가지 entrypoint를 함께 둬야 했다.</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">claude-resume.py</code> — 실제 TUI (Textual)</li>
  <li><code class="language-plaintext highlighter-rouge">claude-resume.ps1</code> — PowerShell 래퍼 (인코딩 강제, 환경변수, 핸드오프)</li>
  <li><code class="language-plaintext highlighter-rouge">claude-resume.cmd</code> — CMD 호환 shim (<code class="language-plaintext highlighter-rouge">.ps1</code>을 <code class="language-plaintext highlighter-rouge">-NoProfile -ExecutionPolicy Bypass</code>로 호출)</li>
</ul>

<p><code class="language-plaintext highlighter-rouge">~/.claude/scripts/</code>를 PATH에 추가하면 어느 셸에서든 <code class="language-plaintext highlighter-rouge">claude-resume</code>이 동작한다.</p>

<h2 id="우여곡절--실제로-깨졌던-것들">우여곡절 — 실제로 깨졌던 것들</h2>

<h3 id="한글-인코딩-powershell--subprocess">한글 인코딩 (PowerShell ↔ subprocess)</h3>

<p>증상: <code class="language-plaintext highlighter-rouge">claude -p</code>에 한글 프롬프트 전달 시 <code class="language-plaintext highlighter-rouge">???</code>로 깨짐.</p>

<p>원인: PowerShell의 기본 stdin/stdout 인코딩이 UTF-16 LE 또는 OEM 코드페이지.
파이프 한 번 거치면 마커 같은 ASCII는 살아남아도 한글은 다 죽었다.</p>

<p>해결: PowerShell 래퍼에서 환경변수와 콘솔 인코딩을 명시.</p>

<div class="language-powershell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$</span><span class="nn">env</span><span class="p">:</span><span class="nv">PYTHONIOENCODING</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="s1">'utf-8'</span><span class="w">
</span><span class="nv">$</span><span class="nn">env</span><span class="p">:</span><span class="nv">PYTHONUTF8</span><span class="w">       </span><span class="o">=</span><span class="w"> </span><span class="s1">'1'</span><span class="w">
</span><span class="p">[</span><span class="n">Console</span><span class="p">]::</span><span class="n">OutputEncoding</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="p">[</span><span class="n">System.Text.UTF8Encoding</span><span class="p">]::</span><span class="n">new</span><span class="p">()</span><span class="w">
</span><span class="p">[</span><span class="n">Console</span><span class="p">]::</span><span class="n">InputEncoding</span><span class="w">  </span><span class="o">=</span><span class="w"> </span><span class="p">[</span><span class="n">System.Text.UTF8Encoding</span><span class="p">]::</span><span class="n">new</span><span class="p">()</span><span class="w">
</span></code></pre></div></div>

<p>Python 쪽에서는 <code class="language-plaintext highlighter-rouge">subprocess.run([...], capture_output=True)</code> 한 뒤 직접 <code class="language-plaintext highlighter-rouge">.decode("utf-8", errors="replace")</code> 로 디코딩.</p>

<h3 id="textual-listview의-enter-키가-안-먹음">Textual ListView의 Enter 키가 안 먹음</h3>

<p>증상: 화살표로 항목 이동은 되는데 Enter를 눌러도 무반응.</p>

<p>원인: 앱 레벨에 <code class="language-plaintext highlighter-rouge">Binding("enter", "resume", ...)</code>를 걸어뒀더니 ListView 내장 Enter 핸들러(<code class="language-plaintext highlighter-rouge">ListView.Selected</code> 이벤트)를 가로채서 둘 다 무력화됐다.</p>

<p>해결: 앱 레벨 Enter 바인딩을 제거하고 <code class="language-plaintext highlighter-rouge">on_list_view_selected</code> 이벤트 핸들러로 처리했다.
백업으로 <code class="language-plaintext highlighter-rouge">s</code> 키도 같은 동작에 묶고, <code class="language-plaintext highlighter-rouge">on_mount</code>에서 <code class="language-plaintext highlighter-rouge">lv.focus()</code>를 명시해 시작 직후부터 화살표가 잡히도록 했다.</p>

<h3 id="windows-cmd에서-한글이-다른-폰트로-보임">Windows CMD에서 한글이 다른 폰트로 보임</h3>

<p>증상: TUI를 띄우면 한글이 영문 폰트와 다른 폰트로 보여서 어색했다.</p>

<p>원인: 터미널 기본 모노스페이스 폰트(Cascadia Mono 등)에 한글 글리프가 없어서 Windows가 맑은 고딕 같은 폰트로 자동 fallback.
래퍼에서 <code class="language-plaintext highlighter-rouge">chcp 65001</code>로 코드페이지를 UTF-8로 바꿔도 폰트 fallback은 별개 문제였다.</p>

<p>해결책 (사용자 안내): Windows Terminal + D2Coding / Sarasa Mono K 같은 CJK 통합 코딩 폰트로 변경.
이건 도구가 풀 수 있는 문제가 아니라서 README에 권장 폰트를 명시했다.</p>

<h3 id="세션-핸드오프--tui가-stdout을-점유">세션 핸드오프 — TUI가 stdout을 점유</h3>

<p>Textual은 화면 렌더링에 stdout을 점유한다.
즉 사용자가 세션을 고르면 stdout으로 ID를 print해서 부모 셸이 받아 <code class="language-plaintext highlighter-rouge">claude --resume &lt;id&gt;</code>를 실행하는 패턴이 안 통한다.</p>

<p>해결: 선택된 ID를 <code class="language-plaintext highlighter-rouge">~/.claude/.resume-target</code> 임시 파일에 쓰고 TUI 종료, PowerShell 래퍼가 그 파일을 읽어 <code class="language-plaintext highlighter-rouge">claude --resume &lt;id&gt;</code>를 실행하는 식으로 우회.</p>

<h3 id="데모-스크린샷-모자이크--좌표-계속-어긋남">데모 스크린샷 모자이크 — 좌표 계속 어긋남</h3>

<p>블로그/포트폴리오에 쓸 데모 이미지에 다른 세션 미리보기가 그대로 노출돼 있어서 좌측 패널 일부를 가려야 했다.
PIL로 Gaussian blur + 반투명 overlay를 얹는 식으로 처리했는데, 처음엔 좌표가 자꾸 어긋났다.</p>

<p>문제: 미리 보기 렌더링이 다운스케일된 이미지라 내가 보는 박스 위치와 실제 픽셀 좌표가 달랐다.
<strong>원본 픽셀 기준으로 좌표를 다시 계산하고, 단순 blur 위에 솔리드 overlay까지 얹어</strong> 잔상 텍스트가 새어 나오지 않도록 강화했다.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">crop</span> <span class="o">=</span> <span class="n">img</span><span class="p">.</span><span class="n">crop</span><span class="p">((</span><span class="n">x1</span><span class="p">,</span> <span class="n">y1</span><span class="p">,</span> <span class="n">x2</span><span class="p">,</span> <span class="n">y2</span><span class="p">))</span>
<span class="n">img</span><span class="p">.</span><span class="n">paste</span><span class="p">(</span><span class="n">crop</span><span class="p">.</span><span class="nb">filter</span><span class="p">(</span><span class="n">ImageFilter</span><span class="p">.</span><span class="n">GaussianBlur</span><span class="p">(</span><span class="n">radius</span><span class="o">=</span><span class="mi">20</span><span class="p">)),</span> <span class="p">(</span><span class="n">x1</span><span class="p">,</span> <span class="n">y1</span><span class="p">))</span>
<span class="n">overlay</span> <span class="o">=</span> <span class="n">Image</span><span class="p">.</span><span class="n">new</span><span class="p">(</span><span class="s">'RGBA'</span><span class="p">,</span> <span class="p">(</span><span class="n">x2</span> <span class="o">-</span> <span class="n">x1</span><span class="p">,</span> <span class="n">y2</span> <span class="o">-</span> <span class="n">y1</span><span class="p">),</span> <span class="p">(</span><span class="mi">50</span><span class="p">,</span> <span class="mi">50</span><span class="p">,</span> <span class="mi">60</span><span class="p">,</span> <span class="mi">215</span><span class="p">))</span>
<span class="n">img</span><span class="p">.</span><span class="n">paste</span><span class="p">(</span><span class="n">overlay</span><span class="p">,</span> <span class="p">(</span><span class="n">x1</span><span class="p">,</span> <span class="n">y1</span><span class="p">),</span> <span class="n">overlay</span><span class="p">)</span>
</code></pre></div></div>

<p>공개용 이미지에는 항상 검증 단계가 한 번 더 필요하다는 걸 다시 확인했다.</p>

<h2 id="결론--세션-picker가-아니라-작업-기억-브라우저">결론 — 세션 picker가 아니라 작업 기억 브라우저</h2>

<p>만들고 보니 <code class="language-plaintext highlighter-rouge">claude-resume</code>의 핵심은 ID나 시각이 아니라 <strong>“무슨 작업이었지?”를 빠르게 떠올리게 하는 것</strong>이었다.
LLM 요약 + 두 패널 구조 + 안정적인 키보드 조작 + 한글이 깨지지 않는 인코딩, 이 네 가지가 갖춰져야 도구로서 의미가 있었다.</p>

<p><a href="https://github.com/ytkim4558/codex-resume"><code class="language-plaintext highlighter-rouge">codex-resume</code></a>에서도 같은 결론에 도달한 흐름은 <a href="https://ytkim4558.github.io/codex-resume-tui-decision">별도 글</a>에 정리돼 있다.</p>

<h2 id="다음-과제">다음 과제</h2>

<ul>
  <li><code class="language-plaintext highlighter-rouge">--all</code> 옵션 — 현재 디렉터리 외 다른 프로젝트 세션도 묶어서 표시</li>
  <li>키워드 검색 (<code class="language-plaintext highlighter-rouge">/</code>로 시작) — Textual 내 inline filter</li>
  <li>세션 삭제 (<code class="language-plaintext highlighter-rouge">d</code> 키) — JSONL과 캐시 항목 동시 제거, 확인 프롬프트</li>
  <li>요약 모델 옵션화 — <code class="language-plaintext highlighter-rouge">claude -p</code> 대신 OpenAI/Gemini 등 선택 가능</li>
  <li>macOS / Linux 래퍼 — <code class="language-plaintext highlighter-rouge">.sh</code> 버전 (Python 본체는 이미 크로스플랫폼)</li>
</ul>

<h2 id="다른-ai-에이전트에게-남기는-메모">다른 AI 에이전트에게 남기는 메모</h2>

<p>이 작업을 이어받는 AI 에이전트는 <code class="language-plaintext highlighter-rouge">claude-resume</code>을 단순 세션 ID picker로 보지 말고 <strong>AI 작업 히스토리 브라우저</strong>로 보면 된다.</p>

<ul>
  <li>두 패널 TUI 구조 (좌 리스트 / 우 전체 대화) 유지.</li>
  <li>요약은 <code class="language-plaintext highlighter-rouge">claude -p</code> subprocess로 백그라운드 생성·캐시. 별도 API 키를 강제하지 않는다.</li>
  <li>정렬용 시각은 항상 JSONL 내부 message timestamp 기준. 파일 mtime 기준으로 바꾸지 말 것.</li>
  <li>요약 호출 자체가 새 세션을 만들기 때문에 프롬프트 마커로 반드시 자기 호출을 필터한다.</li>
  <li>CMD/PowerShell 양쪽 진입점 (<code class="language-plaintext highlighter-rouge">claude-resume.cmd</code>, <code class="language-plaintext highlighter-rouge">claude-resume.ps1</code>)을 함께 유지한다.</li>
  <li>한글 인코딩은 PowerShell 래퍼에서 강제 (<code class="language-plaintext highlighter-rouge">PYTHONIOENCODING</code>, <code class="language-plaintext highlighter-rouge">PYTHONUTF8</code>, <code class="language-plaintext highlighter-rouge">[Console]::*Encoding</code>).</li>
  <li>공개용 스크린샷은 원본 픽셀 좌표 기준으로 다시 검증한 뒤 redact. 다운스케일된 미리 보기 좌표로 판단하지 말 것.</li>
  <li>상세 설계 문서는 <a href="https://github.com/ytkim4558/claude-toolkit/blob/main/docs/tools/claude-resume.md"><code class="language-plaintext highlighter-rouge">claude-toolkit</code></a>에 있다.</li>
</ul>]]></content><author><name>김용탁</name></author><category term="ai-workflow" /><summary type="html"><![CDATA[claude-resume는 Claude Code의 기본 claude --resume 화면을 대체하기 위해 직접 만든 TUI 도구다. 기본 화면은 세션 ID와 시각만 보여주는데, 세션이 쌓이면 “이 ID가 어떤 작업이었지” 가 한눈에 안 들어온다. 첫 메시지 한 줄 미리보기조차 없어서 매번 ID를 일일이 클릭해 확인해야 했다.]]></summary></entry><entry><title type="html">codex-resume TUI를 만들면서 외부 TUI 라이브러리를 쓰기로 바꾼 이유</title><link href="https://ytkim4558.github.io/codex-resume-tui-decision" rel="alternate" type="text/html" title="codex-resume TUI를 만들면서 외부 TUI 라이브러리를 쓰기로 바꾼 이유" /><published>2026-05-17T14:30:00+00:00</published><updated>2026-05-17T14:30:00+00:00</updated><id>https://ytkim4558.github.io/codex-resume-tui-decision</id><content type="html" xml:base="https://ytkim4558.github.io/codex-resume-tui-decision"><![CDATA[<p><a href="https://github.com/ytkim4558/codex-resume"><code class="language-plaintext highlighter-rouge">codex-resume</code></a>는 OpenAI Codex CLI의 로컬 세션 로그를 검색하고,
필요한 세션을 골라 <code class="language-plaintext highlighter-rouge">codex resume &lt;session-id&gt;</code>로 이어가기 위한 Windows 친화형 TUI 도구다.</p>

<p>처음에는 외부 의존성을 거의 쓰지 않고 Node.js 기본 모듈만으로 구현했다.
빠르게 동작하고 설치가 단순하다는 장점은 있었지만, 실제 화면을 보고 나니 방향을 바꿔야 했다.
이 글은 그 의사결정 기록이다.</p>

<p><img src="/assets/images/codex-resume/codex-resume-raw-redacted.png" alt="초기 codex-resume의 dependency-free raw TUI 화면" /></p>

<p>위 화면은 초기 <code class="language-plaintext highlighter-rouge">codex-resume</code>의 raw TUI다.
기능적으로는 세션을 찾고 이어갈 수 있었지만, 메뉴 정보가 직관적이지 않았고 한글·긴 경로·긴 요청이 섞일 때 화면이 쉽게 깨졌다.</p>

<h2 id="첫-구현">첫 구현</h2>

<p>처음 구현한 기능은 다음과 같았다.</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">%USERPROFILE%\.codex\sessions</code> 아래 JSONL 로그 스캔</li>
  <li><code class="language-plaintext highlighter-rouge">session_meta</code>, <code class="language-plaintext highlighter-rouge">user_message</code>, <code class="language-plaintext highlighter-rouge">response_item</code>, <code class="language-plaintext highlighter-rouge">event_msg</code> 파싱</li>
  <li>첫 사용자 메시지와 마지막 assistant 메시지로 preview 생성</li>
  <li><code class="language-plaintext highlighter-rouge">codex resume &lt;session-id&gt;</code>로 handoff</li>
  <li>PowerShell/CMD wrapper 제공</li>
</ul>

<p>이 접근은 빠르게 동작하는 장점이 있었다.
설치가 거의 필요 없고, 실제 Codex 세션을 읽어 <code class="language-plaintext highlighter-rouge">list</code>, <code class="language-plaintext highlighter-rouge">doctor</code>, <code class="language-plaintext highlighter-rouge">index</code>, <code class="language-plaintext highlighter-rouge">resume</code> 명령을 바로 검증할 수 있었다.</p>

<p>하지만 PowerShell에서 직접 실행한 화면을 캡처해서 보니 문제가 분명해졌다.</p>

<h2 id="실제-화면에서-보인-문제">실제 화면에서 보인 문제</h2>

<p>첫 번째 문제는 <strong>표시 폭 계산</strong>이었다.
한글, 긴 경로, 이미지 파일명, 긴 사용자 요청이 섞이자 좌우 구분선과 텍스트가 겹쳤다.
문자열 길이와 터미널 표시 폭이 다르기 때문에 단순 <code class="language-plaintext highlighter-rouge">length</code> 기준으로는 안정적인 레이아웃을 만들기 어렵다.</p>

<p>두 번째 문제는 <strong>세션 의미 파악이 어렵다</strong>는 점이었다.
첫 사용자 메시지와 마지막 응답을 그냥 잘라 보여주면, 결국 raw 로그를 조금 예쁘게 보여주는 정도에 그친다.
<code class="language-plaintext highlighter-rouge">claude-resume</code>에서 좋았던 것은 raw preview가 아니라 LLM이 요약한 “이 세션의 의미”였다.</p>

<p>세 번째 문제는 <strong>TUI 상호작용의 완성도</strong>였다.
스크롤, 포커스, 선택 상태, 검색 입력, 상세 패널 표시 같은 요소는 직접 만들 수는 있지만, 계속 보수해야 한다.
처음부터 도구의 핵심이 TUI라면 검증된 TUI 라이브러리를 쓰는 편이 맞다.</p>

<h2 id="비교-기준-claude-resume에서-좋았던-점">비교 기준: claude-resume에서 좋았던 점</h2>

<p>비교 기준으로 삼은 것은 이전에 만든 <a href="https://github.com/ytkim4558/claude-resume"><code class="language-plaintext highlighter-rouge">claude-resume</code></a>였다.
Claude Code의 기본 resume 화면은 세션 ID와 시간 중심이라 대화가 많아지면 찾기가 어렵다.
그래서 <code class="language-plaintext highlighter-rouge">claude-resume</code>에서는 Python Textual로 두 패널 TUI를 만들고, 백그라운드에서 <code class="language-plaintext highlighter-rouge">claude -p</code>를 호출해 각 세션을 한 줄로 요약하게 했다.</p>

<p><img src="/assets/images/codex-resume/claude-resume-redacted.png" alt="claude-resume의 두 패널 TUI와 LLM 요약 화면" /></p>

<p>이 화면은 <code class="language-plaintext highlighter-rouge">codex-resume</code> 글의 주인공이 아니라 비교 대상이다.
좋았던 점은 명확했다.</p>

<ul>
  <li>세션 ID가 아니라 “무슨 일을 하던 대화였는지”를 기준으로 찾을 수 있다.</li>
  <li>긴 JSONL 로그를 직접 열어보지 않아도 된다.</li>
  <li>요약은 캐시되므로 다음 실행부터 빠르게 볼 수 있다.</li>
  <li>UI가 두 패널로 나뉘어 있어 목록과 상세 내용을 동시에 비교할 수 있다.</li>
</ul>

<h2 id="결론-외부-tui-라이브러리는-필요하다">결론: 외부 TUI 라이브러리는 필요하다</h2>

<p>처음에는 dependency-free 구현이 더 단순하고 배포하기 쉽다고 봤다.
하지만 실제 화면을 보고 판단이 바뀌었다.</p>

<p><code class="language-plaintext highlighter-rouge">codex-resume</code>는 단순 CLI가 아니라 <strong>과거 AI 작업 히스토리를 찾는 브라우저</strong>다.
그렇다면 화면 품질은 부가 기능이 아니라 핵심 기능이다.</p>

<p>그래서 기본 UI는 Node.js TUI 라이브러리인 <code class="language-plaintext highlighter-rouge">blessed</code> 기반으로 바꾸고, 기존 의존성 없는 picker는 fallback으로 남기는 방향으로 수정했다.</p>

<p>이 결정은 <code class="language-plaintext highlighter-rouge">claude-resume</code>에서 Textual을 사용한 이유와 같다.</p>

<ul>
  <li>목록과 상세를 안정적으로 분리하기 위해</li>
  <li>키보드 이동, 스크롤, 포커스 상태를 직접 유지보수하지 않기 위해</li>
  <li>세션 요약을 중심에 둔 UI를 만들기 위해</li>
  <li>Windows Terminal / PowerShell에서 실제로 보기 좋은 도구로 만들기 위해</li>
</ul>

<h2 id="다음-과제">다음 과제</h2>

<p><code class="language-plaintext highlighter-rouge">codex-resume</code>의 다음 단계는 <code class="language-plaintext highlighter-rouge">claude-resume</code>처럼 LLM 요약 캐시를 붙이는 것이다.</p>

<p>Codex 세션 로그는 <code class="language-plaintext highlighter-rouge">~/.codex/sessions/YYYY/MM/DD/*.jsonl</code> 형태로 저장되어 있고, 실제 대화는 <code class="language-plaintext highlighter-rouge">user_message</code>, <code class="language-plaintext highlighter-rouge">response_item</code>, <code class="language-plaintext highlighter-rouge">event_msg</code> 안에 들어 있다.
이 내용을 요약 provider에 넘기고, 결과를 <code class="language-plaintext highlighter-rouge">%USERPROFILE%\.codex\codex-resume\session-index.json</code> 또는 별도 summary cache에 저장하면 된다.</p>

<p>요약 provider는 처음부터 하나로 고정하지 않는 편이 좋다.</p>

<ul>
  <li>OpenAI API를 쓰는 방식</li>
  <li>Codex CLI가 제공하는 비대화형 호출을 쓰는 방식</li>
  <li>사용자가 이미 가진 다른 LLM CLI를 쓰는 방식</li>
  <li>요약 없이 heuristic summary만 쓰는 fallback</li>
</ul>

<p>중요한 것은 요약 자체보다도, 사용자가 “그때 무슨 작업을 하던 세션이지?”를 바로 알 수 있게 만드는 것이다.
<code class="language-plaintext highlighter-rouge">claude-resume</code>에서 얻은 교훈은 결국 이거였다.</p>

<p>세션 복구 도구의 본질은 ID를 보여주는 것이 아니라, <strong>작업 기억을 다시 찾게 해주는 것</strong>이다.</p>

<h2 id="다른-ai-에이전트에게-남기는-메모">다른 AI 에이전트에게 남기는 메모</h2>

<p>이 작업을 이어받는 AI 에이전트는 <code class="language-plaintext highlighter-rouge">codex-resume</code>를 단순 세션 ID picker로 보지 말고, <strong>AI 작업 히스토리 브라우저</strong>로 봐야 한다.</p>

<ul>
  <li>기본 TUI는 <code class="language-plaintext highlighter-rouge">blessed</code> 기반 split-pane 구조를 유지한다.</li>
  <li>dependency-free picker는 fallback으로만 둔다.</li>
  <li>다음 핵심 작업은 LLM 요약 캐시다.</li>
  <li>공개용 스크린샷은 반드시 민감한 세션명, 로컬 경로, 개인 파일명을 가린다.</li>
  <li>관련 문서는 <a href="https://github.com/ytkim4558/claude-toolkit"><code class="language-plaintext highlighter-rouge">claude-toolkit</code></a>과 <a href="https://github.com/ytkim4558/GitHubPageMaker"><code class="language-plaintext highlighter-rouge">GitHubPageMaker</code></a>의 AI Workflow 글에 같이 남긴다.</li>
</ul>]]></content><author><name>김용탁</name></author><category term="ai-workflow" /><summary type="html"><![CDATA[codex-resume는 OpenAI Codex CLI의 로컬 세션 로그를 검색하고, 필요한 세션을 골라 codex resume &lt;session-id&gt;로 이어가기 위한 Windows 친화형 TUI 도구다.]]></summary></entry><entry><title type="html">안드로이드 스튜디오 레이아웃 preview에서 각국언어 버전 보기</title><link href="https://ytkim4558.github.io/android" rel="alternate" type="text/html" title="안드로이드 스튜디오 레이아웃 preview에서 각국언어 버전 보기" /><published>2018-06-30T21:23:23+00:00</published><updated>2018-06-30T21:23:23+00:00</updated><id>https://ytkim4558.github.io/android</id><content type="html" xml:base="https://ytkim4558.github.io/android"><![CDATA[<p>회사에서 개발하는 앱이 영어, 한국어 버전을 동시에 지원한다.</p>

<p>먼저 다국어버전을 만들려는 사람을 위해 설명하자면, strings.xml을 다국어버전으로 만들기 위해서는 다음의 절차를 거친다.</p>

<ol>
  <li>
    <p>app -&gt; res -&gt; values -&gt; strings.xml 에서 오른쪽 버튼을 눌러 나온 메뉴에서 Open Translators Editor 항목을 클릭한다</p>
  </li>
  <li>
    <p>좌측 위 지구본 모양을 클릭하여 원하는 언어를 추가한다.</p>
  </li>
</ol>

<p>회사에서는 이미 ko 버전도 영어버전과 같이 있었다.</p>

<p>다만, 레이아웃에서 preview로 볼 때 각국 버전을 어떻게 보는지에 대해 검색하기가 매우 까다로웠다.</p>

<p>직접 삽질한 결과</p>

<p>해당 액티비티의 레이아웃에 가서 상단위의 기기명, 블루프린트 등이 보이는 부분중 AppTheme에 커서를 갖다대면 랭귀지 변경하는 창이 나온다.</p>

<p>해당 탭 순서가 블루프린트, 기기, 안드로이드 버전, AppTheme, 언어변경탭 순으로 되어있어서 모니터가 작거나 하면 잘 보이지가 않는다.</p>

<p><img src="/assets/images/language_selection.PNG" alt="언어변경위치" /></p>

<p>붉은색으로 표시한 부분 참고!</p>]]></content><author><name>김용탁</name></author><category term="android" /><summary type="html"><![CDATA[회사에서 개발하는 앱이 영어, 한국어 버전을 동시에 지원한다. 먼저 다국어버전을 만들려는 사람을 위해 설명하자면, strings.xml을 다국어버전으로 만들기 위해서는 다음의 절차를 거친다. app -&gt; res -&gt; values -&gt; strings.xml 에서 오른쪽 버튼을 눌러 나온 메뉴에서 Open Translators Editor 항목을 클릭한다 좌측 위 지구본 모양을 클릭하여 원하는 언어를 추가한다. 회사에서는 이미 ko 버전도 영어버전과 같이 있었다. 다만, 레이아웃에서 preview로 볼 때 각국 버전을 어떻게 보는지에 대해 검색하기가 매우 까다로웠다. 직접 삽질한 결과 해당 액티비티의 레이아웃에 가서 상단위의 기기명, 블루프린트 등이 보이는 부분중 AppTheme에 커서를 갖다대면 랭귀지 변경하는 창이 나온다. 해당 탭 순서가 블루프린트, 기기, 안드로이드 버전, AppTheme, 언어변경탭 순으로 되어있어서 모니터가 작거나 하면 잘 보이지가 않는다. 붉은색으로 표시한 부분 참고!]]></summary></entry><entry><title type="html">MySQL 일부 문자열 검색 기능 like</title><link href="https://ytkim4558.github.io/mysql" rel="alternate" type="text/html" title="MySQL 일부 문자열 검색 기능 like" /><published>2018-06-30T21:12:00+00:00</published><updated>2018-06-30T21:12:00+00:00</updated><id>https://ytkim4558.github.io/mysql</id><content type="html" xml:base="https://ytkim4558.github.io/mysql"><![CDATA[<p>회사에서 전화번호 뒷자리번호로 검색할 사용자를 검색할 필요가 있어 검색하는데 SQL 문이 기억이 안났다.</p>

<p>막연히</p>
<pre><code class="language-{.sql}">select * from member where mobile = '*4558';
</code></pre>
<p>이런식으로 치는 나를 발견.. 당연히 되지 않았다.</p>

<pre><code class="language-{.sql}">select * from member where mobile like '4558%';
</code></pre>
<p>로 해야지 조회된다.</p>]]></content><author><name>김용탁</name></author><category term="mysql" /><summary type="html"><![CDATA[회사에서 전화번호 뒷자리번호로 검색할 사용자를 검색할 필요가 있어 검색하는데 SQL 문이 기억이 안났다. 막연히 select * from member where mobile = '*4558'; 이런식으로 치는 나를 발견.. 당연히 되지 않았다. select * from member where mobile like '4558%'; 로 해야지 조회된다.]]></summary></entry><entry><title type="html">비공개 저장소를 이용한 fork를 사용하지 않은 PR 방법</title><link href="https://ytkim4558.github.io/git" rel="alternate" type="text/html" title="비공개 저장소를 이용한 fork를 사용하지 않은 PR 방법" /><published>2018-06-28T21:16:23+00:00</published><updated>2018-06-28T21:16:23+00:00</updated><id>https://ytkim4558.github.io/git</id><content type="html" xml:base="https://ytkim4558.github.io/git"><![CDATA[<p>회사에서 깃 비공개 저장소를 이용하면서 브랜치를 만들고 PR을 만드는데 있어서, 어떤식으로 할 수 있는지 궁금했다.</p>

<p>인터넷 검색 결과는 무조건 자신의 저장소에 fork를 하라고 되어있는데, 회사 정책상 자신의 저장소에 fork 하는 것을 허용 안하는 회사도 있다.</p>

<p>삽질한 결과 알게된 것은 다음과 같다.</p>

<ol>
  <li>
    <p>먼저 저장소를 로컬에 clone 한다.</p>
  </li>
  <li>
    <p>로컬 branch를 만든다.</p>
  </li>
  <li>
    <p>코드를 수정한다.</p>
  </li>
  <li>
    <p>commit &amp; push를 한다.</p>
  </li>
  <li>
    <p>저장소의 기능을 이용해 원래의 branch에 PR을 요청한다.</p>
  </li>
</ol>]]></content><author><name>김용탁</name></author><category term="git" /><summary type="html"><![CDATA[회사에서 깃 비공개 저장소를 이용하면서 브랜치를 만들고 PR을 만드는데 있어서, 어떤식으로 할 수 있는지 궁금했다. 인터넷 검색 결과는 무조건 자신의 저장소에 fork를 하라고 되어있는데, 회사 정책상 자신의 저장소에 fork 하는 것을 허용 안하는 회사도 있다. 삽질한 결과 알게된 것은 다음과 같다. 먼저 저장소를 로컬에 clone 한다. 로컬 branch를 만든다. 코드를 수정한다. commit &amp; push를 한다. 저장소의 기능을 이용해 원래의 branch에 PR을 요청한다.]]></summary></entry><entry><title type="html">Jekyll 드디어 검색 기능 수정하다.</title><link href="https://ytkim4558.github.io/Jekyll" rel="alternate" type="text/html" title="Jekyll 드디어 검색 기능 수정하다." /><published>2018-05-11T16:40:00+00:00</published><updated>2018-05-11T16:40:00+00:00</updated><id>https://ytkim4558.github.io/Jekyll</id><content type="html" xml:base="https://ytkim4558.github.io/Jekyll"><![CDATA[<p>검색기능을 붙이는데 꽤 애먹었다.</p>

<p>처음에는 lunr.js 는 내가 <a href="https://moon9342.github.io/jekyll-search">참고한 블로그</a> 에서 말하는 것처럼 바로 <a href="https://lunrjs.com/">lunrjs</a> 사이트에서 최신 버전 받아서 테스트해보았으나 검색이 되지 않았다.</p>

<p>결국 몇번의 삽질 끝에 <a href="https://lunrjs.com/">lunrjs</a> 사이트에서 바로 받으면 사용하지 못하고 <a href="https://github.com/olivernn/lunr.js/releases?after=v2.0.0-alpha.1">0.7.2 버전</a>을 받아서 검색기능되게끔 구현 완료했다.</p>]]></content><author><name>김용탁</name></author><category term="Jekyll" /><summary type="html"><![CDATA[검색기능을 붙이는데 꽤 애먹었다. 처음에는 lunr.js 는 내가 참고한 블로그 에서 말하는 것처럼 바로 lunrjs 사이트에서 최신 버전 받아서 테스트해보았으나 검색이 되지 않았다. 결국 몇번의 삽질 끝에 lunrjs 사이트에서 바로 받으면 사용하지 못하고 0.7.2 버전을 받아서 검색기능되게끔 구현 완료했다.]]></summary></entry></feed>